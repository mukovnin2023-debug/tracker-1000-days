name: Telegram bot — daily reminders

on:
  schedule:
    - cron: '4,19,34,49 * * * *'
  workflow_dispatch:

jobs:
  remind:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      # ПАУЗА (юридическая): Telegram-бот отключён от продакшена, пока данные хранятся
      # не на российском сервере (152-ФЗ, локализация ПДн). Раскомментировать шаги ниже
      # после переезда бэкенда на российский хостинг.
      - run: echo "Telegram bot paused — see comment above"
      # - run: npm install
      #   working-directory: bot
      # - run: node remind.js
      #   working-directory: bot
      #   env:
      #     TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      #     FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
